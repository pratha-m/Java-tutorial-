import java.util.ArrayList;

public class rearangeElementsBysign {
    public static void main(String[] args) {
        // variety2:alternate numbers (https://www.naukri.com/code360/problems/alternate-numbers_6783445) 
        // If amy of the positive and negative mumbers are left, add
        // them at the end without altering the order.
        //  ArrayList<Integer> pos=new ArrayList<>();
        // ArrayList<Integer> neg=new ArrayList<>();
        // for(int i=0;i<a.length;i++){
        //     if(a[i]>0) pos.add(a[i]);
        //     else neg.add(a[i]);
        // }
        // if(pos.size()>neg.size()){
        //     for(int i=0;i<pos.size();i++){
        //         a[i*2]=pos.get(i);
        //         a[i*2+1]=neg.get(i);
        //     }
        //     int index=neg.size()*2;
        //     for(int i=neg.size();i<pos.size();i++){
        //         a[index]=pos.get(i);
        //         index++;
        //     }
        // }
        // else{
        //     for(int i=0;i<neg.size();i++){
        //         a[i*2]=pos.get(i);
        //         a[i*2+1]=neg.get(i);
        //     }
        //     int index=pos.size()*2;
        //     for(int i=pos.size();i<neg.size();i++){
        //         a[index]=neg.get(i);
        //         index++;
        //     }
        // }
        // return a;

    }
}
